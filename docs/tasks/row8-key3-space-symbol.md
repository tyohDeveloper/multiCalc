# Row 8 Key 3 Label → ␣

## What & Why
Change the label for row 8, key 3 (the space key) from the text `SPC` to the visual space symbol `␣`.

## Done looks like
- The space key in row 8 displays `␣` instead of `SPC`.

## Out of scope
- Any functional or layout changes.

## Tasks
1. In `labels.json`, update the value of `key-r8-3` from `"SPC"` to `"␣"`.

## Relevant files
- `artifacts/rpn-calc/src/config/labels.json:143`
