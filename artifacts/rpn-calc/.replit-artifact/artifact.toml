kind = "web"
previewPath = "/"
title = "MultiCalc"
version = "1.0.0"
id = "artifacts/rpn-calc"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 21164

[services.development]
run = "pnpm --filter @workspace/rpn-calc run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/rpn-calc", "run", "build" ]
publicDir = "artifacts/rpn-calc/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "21164"
BASE_PATH = "/"
